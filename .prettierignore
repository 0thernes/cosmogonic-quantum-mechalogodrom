node_modules
dist
bun.lock
legacy
docs/FILE-MAP.md
# Harness tooling state + co-editor git worktrees (nested repo copies under .claude/worktrees/*).
# Without this, `prettier --check .` recurses into every worktree and a stray style issue there
# breaks the LOCAL gate (CI is unaffected — it runs on a clean checkout). See the multi-context note.
.claude
coverage
# Generated A-Life comparison assets (SVG charts + stats JSON emitted by
# scripts/alife-comparison-stats.ts). Re-running the generator must never trip the formatter.
docs/reports/assets
# Throwaway scratch/verify scripts a session leaves in the root (e.g. __verify_*.ts). They are not in
# tsc's `include` nor oxlint's scope, but `prettier --check .` scans them and a stray one breaks the
# LOCAL gate. Ignore the `__`-prefixed scratch convention so the daily gate stays robust to cruft.
__*
# Diagnostic gate/coverage logs a session tees into the root (already in .gitignore). Mirror them here
# so a captured run log never trips `prettier --check .` on the local gate.
*.log
# MCP tool descriptors (harness state, not product source).
mcps
# MCP server tool descriptors (harness state, not product source).
mcps
# Cursor MCP tool descriptors (not part of the app; break local format:check if scanned).
mcps
mcps/**
**/mcps/**
