node_modules/
dist/
site/
coverage/
*.log
.env
.env.*
.DS_Store
Thumbs.db
verify/
# Throwaway scratch/verify scripts dropped in the repo root by a session (never meant to be committed).
/__*.ts
/__*.js
native/build/
native/build*/
# Local MCP tool descriptors (Cursor harness; not part of the app).
mcps/
# Local Claude/Cursor agent worktrees and harness state.
.claude/
# hacklm-memory tool: undated working copies (.memory/*.md); tool-owned, never part of the app
# (the tracked dated snapshots stay tracked — gitignore only affects untracked files).
.memory/*.md
# Agent harness terminal snapshots (never part of the app).
agent-tools/
terminals/
law_error.txt
law.log
receipts_print.txt
tscout.txt
tsc.log
.gate.log
.gate.baseline.log
.audit-gate.log
verify-mind.ts
*.bmp
.tmp-build-rm-test/
# Session debug dumps (never commit).
checkfinal*.txt
cov.txt
fmt.txt
testout.txt
get-fails.ps1
coverage-current.txt
fails.txt
receipts_print*.txt
smokeout.txt
