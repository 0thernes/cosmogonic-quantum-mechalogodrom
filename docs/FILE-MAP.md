# File Map — the codebase, module by module

_Auto-generated by `bun scripts/gen-filemap.ts` from each module's own doc-header — **do not edit by hand**, fix the module header and re-run. 87 TypeScript modules across 10 directories. The sibling C++ engine lives in `native/src/` (see [ADR-0007](./adr/0007-native-cpp-engine-and-live-physics.md))._

## `src/`

| Module | What it does |
| --- | --- |
| `docs-page.ts` | Entry module for docs.html — initializes mermaid and renders the inline |
| `main.ts` | Browser entrypoint: htmx activation, quality detection, engine boot, |
| `types.ts` | Shared type hub for the Cosmogonic Quantum Mechalogodrom. |
| `world.ts` | Composition root of the simulation: constructs every system against the |

## `src/audio/`

| Module | What it does |
| --- | --- |
| `analysis.ts` | Per-frame audio frequency analysis (CONTRACTS V2 — Quantum Wildbeyond). |
| `engine.ts` | Web Audio engine: procedural music scheduler + a data-driven 100-voice SFX synthesizer |
| `songs.ts` | Procedural soundtrack and SFX catalog data. |

## `src/core/`

| Module | What it does |
| --- | --- |
| `engine.ts` | Owns the WebGL renderer, root scene and perspective camera |
| `postfx.ts` | Post-processing (CONTRACTS V11 + V60) — a guarded EffectComposer chain. |
| `quality.ts` | Quality tier ladder (CONTRACTS V3.1, PANTHEON). |

## `src/logging/`

| Module | What it does |
| --- | --- |
| `audit.ts` | Action audit trail. |
| `logger.ts` | Leveled logger over a single shared ring buffer. |

## `src/math/`

| Module | What it does |
| --- | --- |
| `games.ts` | Game-theory leaf for the TITAN diplomacy layer (CONTRACTS V3.3). |
| `heap.ts` | Array-backed binary heap and a bounded top-K selector. |
| `quantum.ts` | Minimal statevector quantum register (CONTRACTS V2 — Quantum Wildbeyond). |
| `rng.ts` | Deterministic randomness (contract rule 7, Known Bug 9). |
| `scalar.ts` | Scalar math helpers shared by every sim system. |
| `spatial-hash.ts` | Uniform-grid spatial hash over the XZ plane. |

## `src/memory/`

| Module | What it does |
| --- | --- |
| `store.ts` | Versioned localStorage persistence for cross-session preferences. |

## `src/server/`

| Module | What it does |
| --- | --- |
| `ai-sandbox.ts` | Read-only tool sandbox for the Copilot side-chat (CONTRACTS V9 — the LLM shell organ). |
| `copilot.ts` | Copilot LLM bridge (CONTRACTS V9 — the non-deterministic shell organ). |
| `web-search.ts` | WEB SEARCH for the Copilot (V43) — the directive's "in-world AI should search the web for public |

## `src/sim/`

| Module | What it does |
| --- | --- |
| `algorithms.ts` | The 25 sorting-field algorithms — the original 20 (port of legacy `ALGOS`, |
| `analytics.ts` | Rolling-window telemetry science (CONTRACTS V2 — analytics writer). |
| `artifacts.ts` | Artifact field (CONTRACTS V9 — "physical visual effects + artifacts that effect the world"). |
| `atmosphere.ts` | Alien atmosphere — sky dome, breathing haze, particulate air and an aurora curtain |
| `behaviors.ts` | The 26 per-entity behavior fields, ported faithfully from the legacy entity update loop |
| `chaos-field.ts` | CHAOS MODE (CONTRACTS V62) — a toggled storm that turns the calm cosmos into a chaotic, |
| `cognition.ts` | CREATURE COGNITION (CONTRACTS V24) — the pure decision kernel that turns a creature's PERCEPTION + |
| `connectome.ts` | Connectome — the neural-net LineSegments layer. Every update it rebuilds entity-to-entity |
| `constants.ts` | Simulation constants ported verbatim from the legacy monolith |
| `constellations.ts` | Constellation sky-web (V2). A Voronoi diagram + Delaunay triangulation over the 24 |
| `cosmic-web.ts` | Cosmic web (CONTRACTS V11 viz) — a faint, deep far-field lattice of glowing nodes joined by thin |
| `economy.ts` | ECONOMY (CONTRACTS V13) — two competing currencies, two commodities, and a |
| `entities.ts` | Entity lifecycle and per-frame simulation for the organism population. |
| `entity-brain.ts` | PER-ENTITY NEURAL CONTROLLER (V42) — the directive's "50–150 parameter neural network so it's alive |
| `environment.ts` | Non-negative floor for chaos-driven intensity/opacity waves (also seals NaN → 0). O(1). |
| `factions.ts` | Faction archetypes (CONTRACTS V9 — "more things, not just Shoggoths and Puppeteers"). |
| `genome.ts` | Digital genome (CONTRACTS V9 — A-Life genetics, after Creatures/1996 + genetic algorithms). |
| `geometry-cache.ts` | Shared geometry cache — port of legacy `GF`/`cachedGeos` (lines 230-274). |
| `gold-lattice.ts` | Gold lattice (CONTRACTS V11 viz) — a sparse set of large, faint, slowly-turning GOLD wireframe |
| `graph-mind.ts` | GraphMind — the connectome's slow-thinking analytical cortex (CONTRACTS V2). |
| `instanced-entities.ts` | Instanced entity renderer (CONTRACTS V3.1, PANTHEON). |
| `leviathans.ts` | Leviathans (F-BEINGS) — a fourth order of colossal non-human intelligence beyond the shoggoths, |
| `lineage.ts` | Lineage & kinship (CONTRACTS V9 — "offspring and relations… emerging, living, raising, dying"). |
| `lore.ts` | Deterministic cosmic lore (V2). sha256(seed \|\| kind \|\| index) digests index a curated |
| `monolith-temple.ts` | THE MONOLITH TEMPLE (CONTRACTS V63) — the level-100 ascension end-state made physical. When the |
| `morphotypes.ts` | Morphotype generation — port of legacy lines 276-289 (`MT` build loop), |
| `nhi-body.ts` | Alien NHI bodies (CONTRACTS V10 viz) — a dedicated, menacing, morphing form for each launched NHI |
| `nhi-system.ts` | NHI action orchestrator (CONTRACTS V10) — the layer that makes the {@link NhiMind} ACT on the |
| `nhi.ts` | The NHI super-mind (CONTRACTS V10 — the apex non-human intelligence). |
| `phyla.ts` | Phyla — the taxonomic layer above morphotypes (CONTRACTS V3.2, PANTHEON). |
| `puppet-masters.ts` | Puppet-master NPCs — AETHON, SELENE, and KRONOS orbit the arena as glowing tetrahedra and |
| `qcircuit.ts` | QuantumCircuitSystem — the cosmos's quantum "mind" (CONTRACTS V2 — Quantum Wildbeyond). |
| `quantum-lattice.ts` | Quantum lattice (CONTRACTS V11 viz) — a floating neon sacred-geometry anchor: concentric, |
| `quantum.ts` | Quantum cloud — a drifting point field whose particles wobble with a per-particle psi wave, |
| `reaction-diffusion.ts` | Gray–Scott reaction–diffusion — the living skin of the ground (CONTRACTS V2). |
| `shoggoths.ts` | Shoggoth system — three writhing eldritch horrors that drift on lorenz-flavored currents, |
| `singularities.ts` | Cosmological singularities (CONTRACTS V7.4) — the chaos control's repertoire of real |
| `super-body.ts` | THE SUPER CREATURE — the body (V32). The masterful, morphing, many-eyed apex form that renders the |
| `super-creature.ts` | THE SUPER CREATURE — the apex mind of the Mechalogodrom (V31). |
| `super-evolution.ts` | SUPER-CREATURE EVOLUTION (V48) — the self-evolving power/appearance arc the directive demands: |
| `super-mind.ts` | THE SUPER MIND — the apex creature's ~10,000-parameter composite consciousness (V45). |
| `super-wingmen-render.ts` | Wingman-swarm RENDER layer (V47) — draws a {@link WingmanSwarm}'s 100 robots as ONE InstancedMesh |
| `super-wingmen.ts` | THE WINGMAN SWARM — the SUPER CREATURE's escort of mini-robots (V47). |
| `titans.ts` | TITANS (CONTRACTS V3.3) — 10 colossal roaming intelligences running a global economy and |
| `viz3d.ts` | VIZ3D (CONTRACTS V4.2, XENOGENESIS) — floating holographic data sculptures: the in-scene |
| `weather.ts` | Weather system — drives wind, temperature, fog density/color, and tone-mapping exposure |

## `src/sim/ai/`

| Module | What it does |
| --- | --- |
| `brains.ts` | The deterministic classical-AI kernel (CONTRACTS V9 — "how AI was done before the transformer"). |

## `src/ui/`

| Module | What it does |
| --- | --- |
| `access-code.ts` | ACCESS CODE — the pure, testable heart of the cryptographic access puzzle (V34). "Only the Romans |
| `access-puzzle.ts` | ACCESS PUZZLE (V34) — the wild cryptographic GUI that gates the SECOND super creature. A CIA/NSA/ |
| `audit-dock.ts` | AUDIT dock toggle (V51) — moves the audit trail OFF the crowded left column (where it squeezed |
| `center-hud.ts` | CENTER HUD (V56) — unifies the six inspector panels (✦ AI · ❓ HELP · 🗒 AUDIT · ⊞ NEURAL · ⊙ MARKET · |
| `copilot.ts` | Copilot chat panel (CONTRACTS V9 — UI shell, NOT a sim system). |
| `graphs.ts` | Canvas sparkline graphs for the telemetry panel. |
| `help-knowledge.ts` | HELP KNOWLEDGE BASE (V36) — the repo-grounded RAG behind "HELP ME NOW". A curated, DOM-free corpus |
| `help-system.ts` | HELP ME NOW (V36) — the in-world help system the directive asks for, beside DOCS / SPECS / ✦ AI. |
| `hud.ts` | Heads-up display: sector banner (`#sec`), puppet-master toast (`#nm`), the algorithm |
| `input.ts` | Input system: keyboard state, control-pad buttons (`[data-a]`), toolbar buttons |
| `market-ticker.ts` | Market Ticker (CONTRACTS V23) — a self-building panel that makes the economy INSPECTABLE: the live |
| `nhi-observatory.ts` | NHI Neural Observatory (CONTRACTS V15) — a self-building inspection panel that opens a window into |
| `observatory.ts` | Observatory — the live data-viz panel (CONTRACTS V3.5 + V4.3 multi-page XENOGENESIS + |
| `panel-dock.ts` | The PANEL DOCK (V33) — one self-mounting bottom-right MENU BAR that every floating inspector toggle |
| `panels.ts` | Telemetry panel renderer + collapsible panel toggles. |
| `super-panel.ts` | SUPER CREATURE telemetry — the ⬢ ARCHITECT panel (V31). A self-building HUD readout that makes the |
| `superhero-hud.ts` | SUPERHERO HUD (V35) — the player-as-creature overlay the access puzzle unlocks. When ACCESS GRANTED |
| `superhero-state.ts` | SUPERHERO STATE (V35) — the pure, testable progression model behind the player-as-creature mode the |

