node_modules/
dist/
site/
coverage/
*.log
.env
.env.*
.DS_Store
Thumbs.db
verify/
# Throwaway scratch/verify scripts dropped in the repo root by a session (never meant to be committed).
/__*.ts
/__*.js
native/build/
native/build*/
# Local MCP tool descriptors (Cursor harness; not part of the app).
mcps/
# Agent harness terminal snapshots (never part of the app).
terminals/
agent-tools/
# Stale local scratch / console captures dropped in the repo root by a session.
law_error.txt
receipts_print.txt
tscout.txt
check-out.txt
verify-mind.ts
*.bmp
