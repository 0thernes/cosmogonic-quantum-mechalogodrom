# 500-Point Inspection

A standing audit of the Cosmogonic Quantum Mechalogodrom: **25 sections × 20 checkpoints = 500
points**. Each point carries a verdict and concrete evidence (a file, a symbol, a test, or a
measured fact). This is the bar the repository is held to — "professional grade, passes scrutiny."

Verdicts: **✅ PASS** (meets the bar) · **🟡 WARN** (acceptable for a local single-author PoC, but
a named limitation or accepted risk) · **❌ FAIL** (a defect — must be fixed or explicitly waived).

| Verdict   | Count |
| --------- | ----- |
| ✅ PASS   | 484   |
| 🟡 WARN   | 16    |
| ❌ FAIL   | 0     |
| **Total** | 500   |

Scope: every folder, every file, the public surface of every module. Last full sweep: release
`0.8.0`; §13.258, §17.339 + §21.420 promoted to ✅ in `0.9.0` (a `prefers-reduced-motion`
accommodation, a CI coverage gate, and a CycloneDX SBOM shipped). The audit is re-run before each
tagged release; deltas land in [CHANGELOG.md](../CHANGELOG.md).

> Provenance: this list is the human-readable companion to the binding
> [MODULE-CONTRACTS.md](./MODULE-CONTRACTS.md) and the [COMPLEXITY.md](./COMPLEXITY.md) budget.
> Where a point cites "contract Vn", see that file's section. Where it cites a Known Bug number,
> see the contract's Known Bugs table (the legacy defects fixed in the port).

---

## 1. Repository Structure & Layout

1. ✅ Single coherent root; source under `src/`, tests under `tests/`, benches under `bench/`, docs under `docs/` — no stray top-level code files.
2. ✅ `src/` is layered by concern: `core/`, `math/`, `sim/`, `audio/`, `ui/`, `logging/`, `memory/`, `styles/` — directory = bounded context.
3. ✅ The composition root (`src/world.ts`) is the only module that wires systems together; leaf modules never import it (acyclic).
4. ✅ Pure math primitives (`src/math/`) depend on nothing in `sim/`/`ui/` — reusable and unit-testable in isolation.
5. ✅ Each `tests/*.test.ts` maps to exactly one source module by name (e.g. `heap.test.ts` ↔ `math/heap.ts`).
6. ✅ Each `bench/*.bench.ts` covers a leaf hot path and is aggregated by `bench/index.ts`.
7. ✅ The verbatim legacy original is preserved at `legacy/cosmogonic-quantum-mechalogodrom.html` and never edited.
8. ✅ Provenance of the single-file ancestor is documented under `legacy/opus-4.7-origin/` with a `PROVENANCE.md`.
9. ✅ The self-contained p5.js lab lives at `lab/quantum-wildbeyond.html`, deliberately separate from the bundled app.
10. ✅ Architecture decisions are captured as numbered ADRs in `docs/adr/0001..0005`.
11. ✅ Reference material (math-library catalog, domain CSV) is isolated under `docs/reference/`.
12. ✅ The three "master" steering XMLs live under `masters/`, out of the build path.
13. ✅ No `node_modules`, `dist`, or editor cruft is tracked — `.gitignore` covers them.
14. ✅ File naming is consistent kebab-case for modules; PascalCase only for exported classes.
15. ✅ No file exceeds a comprehensible size for its role; the largest (`ui/observatory.ts`, ~2.1k LOC) is a single cohesive rendering surface, not a god-object.
16. ✅ Root config files (`tsconfig`, `bunfig`, `.prettierrc`, `.editorconfig`, `.gitattributes`) are present and minimal.
17. ✅ `docs/` cross-links form a navigable graph (README → ARCHITECTURE → CONTRACTS → COMPLEXITY → ERD/ERM/ERP).
18. ✅ No duplicate or near-duplicate source files; the redundant `ui/touch.ts` twin was removed (contract note).
19. ✅ Directory depth stays shallow (≤ 3 under `src/`), keeping import paths readable.
20. ✅ `dist/` is gitignored, not committed — it is a pure build output, regenerated by `bun run build` locally and in CI (where it is uploaded as a retained artifact), consistent with §1.13.

## 2. Build System & Tooling

21. ✅ Bun is the single runtime, bundler, test runner, and package manager — one toolchain, no Webpack/Vite/Jest sprawl.
22. ✅ `bun run build` (`scripts/build.ts`) produces a minified `dist/` from `index.html` + `docs.html` with the Tailwind plugin wired explicitly.
23. ✅ The build fails loudly: non-zero exit + logged messages on `result.success === false`.
24. ✅ Dev server (`bun --hot server.ts`) gives HMR; documented cwd requirement avoids the HTML-import bundler trap.
25. ✅ Tailwind v4 via `bun-plugin-tailwind`; dev uses `bunfig.toml [serve.static]`, prod wires the plugin in `build.ts` (the two paths are reconciled, per build.ts header).
26. ✅ Prettier is the single formatter; `prettier-plugin-tailwindcss` keeps class order canonical.
27. ✅ oxlint is the linter (fast, zero-config-drift); run over `src server.ts tests bench scripts`.
28. ✅ `bun run check` is the one gate: format → typecheck → lint → test → build, in fail-fast order.
29. ✅ The gate is reproducible: pinned Bun version in CI matches local (`1.3.14`).
30. ✅ `bun install --frozen-lockfile` in CI guarantees the lockfile is authoritative.
31. ✅ `bun.lock` is committed and current.
32. ✅ Scripts in `package.json` are named by intent (`dev`, `build`, `typecheck`, `lint`, `test`, `bench`, `check`).
33. ✅ `.editorconfig` pins indentation/charset across editors.
34. ✅ `.gitattributes` pins `eol=lf` to defeat autocrlf phantom-dirty diffs (cross-referenced in project memory).
35. ✅ `.prettierignore` keeps generated/vendored content out of the formatter.
36. ✅ No build step requires network access beyond the initial dependency install.
37. ✅ The production bundle is fully static — deployable to any file host or object store.
38. ✅ Source maps / minification handled by Bun's `minify: true`; no hand-rolled pipeline.
39. ✅ Bench tooling (`mitata`) is a devDependency, never shipped.
40. 🟡 No cross-platform CI matrix (Linux only); local dev is Windows, so a Linux-only gate could miss an OS-specific path bug — mitigated by `-LiteralPath` discipline and LF normalization.

## 3. TypeScript Type Safety

41. ✅ `strict: true` — the whole strict family is on.
42. ✅ `noUncheckedIndexedAccess: true` — every array index is `T | undefined` and handled (see the `!`-with-invariant-comment idiom).
43. ✅ `noImplicitOverride`, `noFallthroughCasesInSwitch` — structural footguns disabled.
44. ✅ `noUnusedLocals` + `noUnusedParameters` — dead bindings are a compile error, not a smell.
45. ✅ `verbatimModuleSyntax` — `import type` vs value imports are explicit; no accidental runtime imports.
46. ✅ `moduleResolution: bundler` + `module: Preserve` match the Bun runtime exactly.
47. ✅ No `any` in the codebase except where narrowing from `unknown` at trust boundaries (e.g. `parseAuditBody`).
48. ✅ No `@ts-ignore` / `@ts-expect-error` suppressions.
49. ✅ Public module surfaces export named types (`SimContext`, `Entity`, `ObservatorySnapshot`, `Less<T>`).
50. ✅ `tsc --noEmit` is part of the gate and passes clean.
51. ✅ Generic data structures are parameterized, not stringly-typed (`BinaryHeap<T>`, `SpatialHash<T>`).
52. ✅ Discriminated unions / enums used for finite state (strategy, typeId, setGroup) rather than magic numbers without names.
53. ✅ `types.ts` centralizes shared shapes; systems import from it, not from each other's internals.
54. ✅ DOM lib types enabled (`DOM`, `DOM.Iterable`) — canvas/WebGL typed, no `as HTMLCanvasElement` guessing where avoidable.
55. ✅ Bun types (`@types/bun`) provide `Bun.serve`/`Bun.file` typing.
56. ✅ three.js + d3-delaunay typed via `@types/three`, `@types/d3-delaunay`.
57. ✅ Index-signature reads from external objects (e.g. graphology `ranks[key]`) use `?? 0` fallbacks.
58. ✅ Function signatures are total — no implicit `undefined` returns where a value is expected.
59. ✅ `useDefineForClassFields` matches modern class-field semantics.
60. ✅ No structural type-laundering through `as unknown as T` anywhere in `src/`.

## 4. Determinism & Reproducibility

61. ✅ All simulation randomness flows through a single seeded `mulberry32` stream on `SimContext.rng`.
62. ✅ `Math.random` is banned in sim logic (contract rule); the gate would surface a reintroduction.
63. ✅ Audio randomness runs on a _forked_ deterministic stream (`mulberry32((seed ^ 0xa0d10) >>> 0)`), so it never perturbs sim draws.
64. ✅ Louvain community detection consumes `ctx.rng` exclusively — one seed, one tribal history (`graph-mind.ts`).
65. ✅ PageRank top-K selection is order-total (rank desc, ties by ascending index) → identical set every run (`heap.ts` + `graph-mind.ts`).
66. ✅ Lore names derive deterministically from the seed via sha256 (`@noble/hashes`).
67. ✅ A golden same-seed determinism test pins integrated state (`tests/determinism.test.ts`).
68. ✅ Quantum measurement collapses via the seeded stream (`QuantumRegister.measure(rng)`), not `Math.random`.
69. ✅ Object key iteration relies on the JS integer-key ordering spec (documented at each use), not insertion luck.
70. ✅ No wall-clock reads in sim logic; time advances by the rAF `dt` passed in, clamped ≥ 0.
71. ✅ rAF `dt` clamp (`Math.max(0, …)`) prevents a negative first-frame delta from NaN-poisoning Catmull-Rom curves.
72. ✅ Tier ladder is deterministic per device: `targetEntities === maxEntities` on every tier (no adaptive jitter).
73. ✅ Ultra-only perf levers are gated `maxEntities > 5000`, so ≤5k tiers remain byte-identical across runs.
74. ✅ Reaction-diffusion is a pure function of its two Float32Array fields + documented params.
75. ✅ Spatial-hash cell keys are integer and truncation-stable (`| 0`), so neighbor sets are reproducible.
76. ✅ Tests seed every random generator explicitly (`mulberry32(seed)`), never the ambient clock.
77. ✅ Benchmarks run on fixed `mulberry32(42)` inputs so a regression is signal, not noise.
78. ✅ No floating reliance on `Set`/`Map` iteration order for sim outcomes (only for idempotent UI effects).
79. ✅ Persistence round-trips are value-stable (the memory store re-reads what it wrote).
80. ✅ Determinism is a first-class contract concern (rule 5), not an emergent accident.

## 5. Time Complexity & Algorithms (DSA)

81. ✅ Neighbor queries use a uniform-grid spatial hash: O(C + k) per query instead of O(n) scans (`math/spatial-hash.ts`).
82. ✅ Top-K PageRank halo uses a bounded min-heap: O(V log K) instead of O(V log V) full sort (`math/heap.ts`).
83. ✅ The heap is a reusable primitive (`BinaryHeap<T>`) with O(log n) push/pop/replaceRoot — not a one-off.
84. ✅ `selectTopK` is O(n log k) time, O(k) space, and provably returns the stable-sort top-K (parity test).
85. ✅ The grid is rebuilt every other frame (O(n) amortized to O(n/2) per frame).
86. ✅ Connectome cadence backs off with population (every 2nd/3rd frame above thresholds) to bound its O(n·k) cost.
87. ✅ GPU buffer uploads are range-scoped (`addUpdateRange`) — O(links) not O(capacity) (Known Bug 13 fix).
88. ✅ Quantum register gates are O(2^n) with n ≤ 8 hard-capped (32 amplitudes at n=5) — bounded, not unbounded.
89. ✅ Reaction-diffusion is O(SIZE²) on a fixed grid, cadenced every 2nd frame, measured < 0.5 ms at 128².
90. ✅ Voronoi point-location for sector naming is O(log n) via d3-delaunay `find`, built once at O(n log n).
91. ✅ Analyser poll is O(B) = O(128) per frame; analytics regression is O(W) = O(120) every 60th frame.
92. ✅ Sort-visualization step batches 6–28 swaps/frame; per-step work documented O(1)–O(n) worst case.
93. ✅ Audit ring push/render are O(1)/O(n≤200) — constant-bounded by design.
94. ✅ Sparkline is a rolling O(w) draw over a 100-sample window; push is O(1).
95. ✅ Every hot path's Big-O is documented in JSDoc AND tabulated in `COMPLEXITY.md` (single source of truth).
96. ✅ Heavy passes (Louvain 240f, PageRank 600f offset 300) are scheduled to never share a frame.
97. ✅ No accidental quadratic loops in per-frame code (audited; the only O(n) scan — shoggoth consumption — is a cold path firing every 200–500 ticks).
98. ✅ Big-O claims are backed by `mitata` benchmarks for the leaf paths (`bench/`).
99. ✅ The heap bench demonstrates the top-K win at V=10,000, K=20 against the prior sort+slice.
100.  🟡 Shoggoth nearest-victim remains a linear O(n) scan; converting to a spatial query was evaluated and deferred (cold path; the radius-bounded grid query risks tie-break nondeterminism for negligible gain).

## 6. Memory & Allocation Discipline

101. ✅ Contract rule: per-frame `update()` bodies perform zero allocations; documented per system.
102. ✅ Spatial-hash `query()` returns a shared buffer valid until the next call (Known Bug 5 fix — legacy allocated per call).
103. ✅ Cell arrays are pooled across `clear()` cycles — steady-state rebuilds allocate nothing.
104. ✅ The sort step copies into a pre-allocated `Float32Array` (Known Bug 4 fix — legacy built a fresh object per frame).
105. ✅ Module-level scratch vectors (`V1`, shared `Color`) are reused, never re-created in loops.
106. ✅ Quantum register reuses a probability `Float64Array`; qcircuit reuses a 32-entry bands `Float32Array`.
107. ✅ Reaction-diffusion ping-pongs two Float32Array pairs — no per-step buffer churn.
108. ✅ The analyser reuses a `Uint8Array` and returns a reused bands object.
109. ✅ Analytics rings are three pre-allocated 120-sample buffers.
110. ✅ Curve sampling uses `getPointAt(t, target)` into a target vector — no Vector3 per packet (Known Bug 12 fix).
111. ✅ InstancedMesh pools back 10k entities without per-entity scene-graph nodes.
112. ✅ The audit ring is a fixed 200-slot circular buffer; hostile payloads can never grow it.
113. ✅ `BinaryHeap.clear()` retains backing-array capacity for reuse (documented).
114. ✅ Logger ring is a fixed 512-entry buffer.
115. ✅ Telemetry element refs are cached once in the constructor, not re-queried per frame (Known Bug 4 fix).
116. ✅ String building in hot paths is avoided; UI text writes happen on cadence (every 8th frame), not every frame.
117. ✅ Geometry is shared via a geometry cache (~41 shared geometries) rather than per-entity allocation.
118. ✅ No closures are allocated inside per-frame loops where a hoisted function or scratch would do.
119. ✅ GC-pressure regressions are explicitly called out as a contract violation to watch in profiling.
120. ✅ `selectTopK` allocates only an O(k=20) heap on a 600-frame cadence — negligible, documented.

## 7. Rendering & three.js Correctness

121. ✅ `THREE.ColorManagement.enabled = false` is set in `main.ts` before any `Color` is constructed (whiteness-regression root cause #1 fixed).
122. ✅ Light units corrected for the r128→0.184 migration (`LEGACY_LIGHT_GAIN = π`, `POINT_LIGHT_GAIN = π/2`, decay 0) — root cause #2 fixed.
123. ✅ `LinearSRGBColorSpace` chosen deliberately to match the legacy look.
124. ✅ InstancedMesh used for the 10k-entity population — one draw call per morph pool, not 10k meshes.
125. ✅ Instance matrices/colors flagged `needsUpdate` only when changed.
126. ✅ Buffer attribute uploads are range-scoped, never full-buffer re-uploads (Known Bug 13).
127. ✅ The render loop is the single rAF in `main.ts`; systems are pure update steps invoked from it.
128. ✅ Camera/controls math is allocation-free per frame (scratch vectors).
129. ✅ Atmosphere sky dome + fog are driven by the weather system, lerped smoothly (no per-frame snap).
130. ✅ Emissive halos (rank boost) lerp back to morph baseline between passes — no stuck-on state (rank-halo restore contract, fixed dead-code bug).
131. ✅ Holographic 3D analytics (`viz3d`) render on documented cadence, not every frame.
132. ✅ Geometry cache prevents duplicate geometry uploads.
133. ✅ Materials are shared per morphotype, not per entity.
134. ✅ The reaction-diffusion U field uploads as a `DataTexture` (ground emissiveMap), not a CPU redraw.
135. ✅ Draw ranges are set tightly (`setDrawRange(0, ti*2)`) so unused tendril segments aren't drawn.
136. ✅ No `console.log` in the render loop; logging is event-driven through the logger ring.
137. ✅ WebGL context loss is survivable (state is reconstructable from the sim, which is the source of truth).
138. ✅ Render-mode tests pin instanced vs non-instanced parity (`tests/render-modes.test.ts`, `tests/instanced.test.ts`).
139. ✅ Pixel-level "is it actually drawing" is verifiable via `gl.readPixels` inside rAF (documented technique).
140. 🟡 Preview screenshots hang when the tab is backgrounded (rAF pauses on `document.hidden`) — a tooling quirk, worked around via in-rAF framebuffer sampling, not an app defect.

## 8. Numerical Stability (NaN / Inf)

141. ✅ Lorenz-attractor samples are clamped to `±LORENZ_BOUND` before feeding quadratic cross-terms (blow-up sealed in both the entity behavior and the shoggoth drift).
142. ✅ Connectome activation is clamped to ±4 to stop divergence to Infinity.
143. ✅ Behavior position overflow at max chaos is contained by a bounded impulse, not an unbounded spring.
144. ✅ rAF `dt` is clamped ≥ 0 — no negative-delta NaN propagation.
145. ✅ The memory store rejects NaN, ±Infinity, and non-numbers on read (`finite`-narrowing helper).
146. ✅ Audit `ts` outside the valid `Date` range falls back to wall clock (prevents `toISOString` throw).
147. ✅ A dedicated NaN-stability test suite drives systems at sustained max chaos (`tests/nan-stability.test.ts`).
148. ✅ Quantum amplitude math stays normalized (probabilities sum-to-one assertions in tests).
149. ✅ Vector normalizations guard against zero-length (no `NaN` from `normalize()` of a zero vector in hot paths).
150. ✅ Division-by-density / by-count paths use guarded denominators.
151. ✅ HSL/RGB conversions stay within `[0,1]` domains.
152. ✅ Reaction-diffusion params are kept in the stable Gray-Scott regime; weather coupling is bounded.
153. ✅ Exposure/tone accumulation is bounded (replaced an earlier whitening accumulation with a bounded rig).
154. ✅ Entity lifespan math (`age > life * tempMod`) cannot underflow to spurious immortality (tempMod bounded).
155. ✅ Energy/wealth in the titan economy is clamped to `[0,100]`.
156. ✅ Sort values are finite floats; the sorted-fraction metric uses bounded indices.
157. ✅ No `parseInt`/`parseFloat` on untrusted input without finiteness checks.
158. ✅ Tests assert "no NaN in any exposed buffer" after long runs for the heavy substrates.
159. ✅ Catmull-Rom `getPointAt` inputs are kept in `[0,1]` and finite.
160. ✅ The golden determinism test would catch any NaN-induced divergence as a hash mismatch.

## 9. Simulation Systems & Behaviors

161. ✅ 10 lore-named phyla × 25 morphotypes (+ ~1% wildcard outliers) — a structured, named taxonomy (`sim/phyla.ts`, `sim/lore.ts`).
162. ✅ 26 behaviors, assigned per morphotype, each documented with its complexity (`sim/behaviors.ts`).
163. ✅ 10 game-theoretic titans run an iterated-PD/replicator economy + war (`sim/titans.ts`, `math/games.ts`).
164. ✅ Shoggoths (3) tug and consume via grid queries; puppet-masters trigger remorph/weather events.
165. ✅ The connectome is a living neural web with activation, decay, and community palette.
166. ✅ GraphMind runs real graph science (Louvain + PageRank) over the live network on cadence.
167. ✅ Quantum cloud + register + circuit form a coherent quantum substrate feeding visuals and gates.
168. ✅ Reaction-diffusion provides an emergent living ground texture coupled to deaths and weather.
169. ✅ Constellations name sky sectors via Voronoi; tribes name communities — lore is data-driven.
170. ✅ Atmosphere + weather give the biome a sky, fog, wind, and temperature with lifespan effects.
171. ✅ Entity birth/death/split lifecycle is tested (`tests/entities-death.test.ts`, phyla/lore tests).
172. ✅ Behavior assignment is deterministic (`id mod 26`, overridable) — documented in the ERD.
173. ✅ Theory behaviors are staggered (`(frame + i) % 2`, stride 3 on ultra) to halve their per-frame population.
174. ✅ Singularities/monoliths/dioramas are fixed structural features with O(1) updates.
175. ✅ Each system's update is a pure step over its own state; cross-system effects flow through documented write-backs (e.g. `setGroup`).
176. ✅ Sentience index aggregates network/diversity signals into a single telemetry readout.
177. ✅ Systems degrade gracefully at low tiers (counts scale, cadences hold).
178. ✅ The titan economy and war are unit-tested for conservation/where-applicable invariants (`tests/games.test.ts`).
179. ✅ Analytics emits population z-score "omens" (|z| > 2.5), throttled to once per 30 s.
180. ✅ The sim is the source of truth; rendering/audio/UI are read-only projections of it.

## 10. Audio Engine & Synthesis

181. ✅ Web Audio graph is constructed lazily on first user gesture (`world.unlock()`), respecting autoplay policy.
182. ✅ 6 dramatic songs with deepened synthesis (sub-bass, detuned third voice, arpeggiation, filter LFO, per-song dynamics).
183. ✅ Per-algorithm selection cue: a unique tone per sorting algorithm (`audio/engine.ts` `cue(idx, total)`).
184. ✅ Audio randomness is on a forked seeded stream — never perturbs the sim (determinism-safe).
185. ✅ The analyser tap is allocation-free (reused `Uint8Array`, exponential smoothing) and O(128)/frame.
186. ✅ SFX vs music gain are separated, independently mutable (`sfxGain`, `_sfxOn`).
187. ✅ Songs are data (`audio/songs.ts`), the engine is the interpreter — adding a song is data, not code surgery.
188. ✅ Audio analysis output (bands) feeds visuals (constellation treble pulses) via documented coupling.
189. ✅ Oscillator/envelope lifetimes are bounded; no leaking nodes accumulate.
190. ✅ The audio system is unit-tested (`tests/songs.test.ts`, `tests/audio-analysis.test.ts`).
191. ✅ Gain ramps use `setTargetAtTime`/linear ramps — no click/pop from instantaneous jumps.
192. ✅ Frequency math is rooted musically (G3-rooted ~3-octave cue spread) — intentional, not arbitrary.
193. ✅ The engine no-ops cleanly when audio is unavailable/locked (guards on `ctx`/`out`).
194. ✅ Band data zeros before `init()` so first-frame visuals don't read garbage.
195. ✅ No audio work runs before unlock — silent, allocation-light idle.
196. ✅ Master dynamics keep output within a sane loudness band (no clipping by design).
197. ✅ Song switching is deterministic and persisted (`songIdx` in state).
198. ✅ The cue's two-voice structure (body + shimmer partial) is documented in the engine.
199. ✅ Audio constants (gains, envelopes) are named, not magic.
200. 🟡 Loudness is not formally normalized to an LUFS target across songs; balanced by ear and per-song dynamics (acceptable for a PoC).

## 11. UI, Observatory & Visualization

201. ✅ The Observatory is a 4-page analytics surface (Overview/Variance/Ecology/Conflict) over 16 canvases.
202. ✅ Each page draws on a documented cadence (observatory every 18f), not every frame.
203. ✅ In-canvas titles, legends, gridlines, and a title-band inset keep labels off the data (0.6.0 fix).
204. ✅ Titan Roster + Resources panels reflow (1-col/2-col fallback, truncate-to-width) — no overlap (0.6.0 fix).
205. ✅ A visible 25-algorithm picker exists with per-row progress + per-algorithm light + sound.
206. ✅ Telemetry HUD shows live counts (population, step/swap counters, sentience) updated on cadence.
207. ✅ Sparklines render rolling windows with cached 2D contexts.
208. ✅ The `/docs` route is a local GitHub-Pages-style report (file tree + ERD/ERM/ERP + mermaid).
209. ✅ Mermaid diagrams avoid the `;`-in-label parse crash (commas/dashes only) — verified to render.
210. ✅ Panels are edge-docked slide-out sheets on mobile (handles per edge; Escape closes).
211. ✅ The HTMX audit panel polls `/api/audit` every 5 s and renders server-escaped HTML.
212. ✅ Canvas elements carry `role="img"` + descriptive `aria-label`.
213. ✅ Observatory pages use `role="tablist"`/`role="tab"`/`aria-selected` for the tab control.
214. ✅ Page-0 ids/aria-labels are kept verbatim so `panels.ts`/`hud.ts`/`observatory.ts` selectors stay stable.
215. ✅ Visualization math (variance/diversity helpers) is pure and unit-tested (`tests/analytics.test.ts`).
216. ✅ The Observatory snapshot is a typed, testable data structure (`ObservatorySnapshot`, 597-line test).
217. ✅ Colors come from the design-system tokens (`docs/DESIGN-SYSTEM.md`), not ad-hoc hexes.
218. ✅ Text truncation is width-aware (`truncateToWidth`), not naive substring.
219. ✅ The lab is a 4-page p5.js WEBGL surface with 12 3D boards per page (sound + reactive).
220. ✅ UI work never blocks the sim; it reads snapshots, never mutates sim state.

## 12. Input & Controls (mouse / touch / responsive)

221. ✅ Mouse navigation: drag-look + scroll-wheel zoom (`ui/input.ts`).
222. ✅ Touch: a look pad + a radial control wheel for coarse-pointer devices.
223. ✅ Pinch-to-zoom is implemented (two-finger spread via `Math.hypot`, look suppressed during pinch).
224. ✅ Held-input state is reset cleanly (`clearHeldInput`) so a lost pointer-up doesn't stick.
225. ✅ Responsive from phone to 43″ TV — tier ladder + edge-docked sheets + fluid layout.
226. ✅ Coarse vs fine pointer is detected; touch affordances appear only where relevant.
227. ✅ Input gain constants (zoom, pinch) are named and tuned, not magic.
228. ✅ The first user gesture unlocks audio (one-time), satisfying browser autoplay policy.
229. ✅ Keyboard: Escape closes sheets; tabs are keyboard-activatable (Enter/role=tab semantics).
230. ✅ Input handlers are attached to specific elements (`#c`, pads), not global where avoidable.
231. ✅ No input handler does heavy work synchronously; they set state the rAF loop reads.
232. ✅ Pointer math is allocation-free (scratch, not new vectors per move).
233. ✅ Look/zoom are clamped to sane ranges (no flip-over or inside-out camera).
234. ✅ Multi-touch bookkeeping tracks the right touch identifiers (no cross-finger glitch).
235. ✅ The control surface is documented as canonical in `ui/input.ts` (the redundant twin was removed).
236. ✅ Sheets are reachable and dismissable on small screens without trapping focus.
237. ✅ Theme-color meta + dark palette suit OLED/large displays.
238. ✅ Viewport meta is set for correct mobile scaling.
239. 🟡 No gamepad support (out of scope for the stated targets).
240. 🟡 No automated input/interaction e2e test (Playwright-class); input is exercised manually + via unit-tested state transitions.

## 13. Accessibility (a11y)

241. ✅ `<html lang="en">` is set.
242. ✅ `<title>` is present and descriptive.
243. ✅ `<meta name="description">` and Open Graph tags are present.
244. ✅ `theme-color` meta set for UA chrome theming.
245. ✅ Interactive controls have `aria-label`s (sheet toggles, panel collapses).
246. ✅ The observatory tabs use a correct `role="tablist"` / `role="tab"` / `aria-selected` pattern.
247. ✅ `aria-controls` / `aria-expanded` wire toggles to the regions they govern.
248. ✅ Decorative elements use `aria-hidden="true"` (grips, ornaments).
249. ✅ Data canvases expose meaning via `role="img"` + `aria-label`.
250. ✅ Landmark labels (`aria-label="Telemetry"`, `"Observatory"`) name major regions.
251. ✅ Color is not the sole carrier of meaning in the HUD (labels + numbers accompany hues).
252. ✅ The dark palette maintains readable contrast for primary text (design-system tokens).
253. ✅ Escape-to-close and visible focus affordances support keyboard users.
254. ✅ No positive `tabindex` traps; natural DOM order is the tab order.
255. ✅ Motion-heavy visuals are confined to the canvas; UI chrome is stable.
256. ✅ Text is real text (not baked into images) wherever it conveys data.
257. ✅ The `/docs` report is plain semantic HTML, screen-reader navigable.
258. ✅ `prefers-reduced-motion` is honored — the UI chrome's animations/transitions collapse to near-instant (`app.css`); the WebGL canvas stays live by design, and the TIME control can pause the whole sim (timeScale 0).
259. 🟡 No automated a11y audit (axe-core) in CI (manual review only).
260. 🟡 WCAG contrast is not formally measured against AA for every secondary/disabled state.

## 14. Server & API

261. ✅ Single Bun fullstack server (`server.ts`); routes declared as data in `Bun.serve({ routes })`.
262. ✅ Routes are explicit and documented in the file header (`/`, `/docs`, `/lab`, `/api/health`, `/api/audit`).
263. ✅ `GET /api/health` returns `{ ok, uptime, version }` — a real liveness probe.
264. ✅ `GET /api/audit` returns an escaped HTML fragment for HTMX (newest first).
265. ✅ `POST /api/audit` validates, bounds, and stores into an O(1) ring.
266. ✅ Unknown routes return a clean 404 (catch-all `fetch`).
267. ✅ Every request is logged once with method, path, and status.
268. ✅ Content-Type is set correctly per route (`text/html; charset=utf-8`, JSON via `Response.json`).
269. ✅ The lab is served as a static file, deliberately un-bundled (CDN-by-design).
270. ✅ `development` mode is keyed off `NODE_ENV`, so prod doesn't leak dev affordances.
271. ✅ Port is configurable via `PORT` env with a 3000 default.
272. ✅ The server holds no unbounded state — the only store is the fixed-size ring.
273. ✅ Rendering a bad audit slot is fault-isolated (one bad entry → placeholder `<li>`, not a 500).
274. ✅ The health version is now derived from `package.json` at startup (no drift) — see §25.
275. ✅ No secrets, tokens, or credentials are read or required by the server.
276. ✅ JSON parsing is wrapped in try/catch with a 400 on failure.
277. ✅ Request body reads are wrapped in try/catch with a 400 on failure.
278. ✅ The server is stateless across restarts except the in-memory ring (by design — it's a PoC telemetry feed).
279. ✅ No filesystem writes from request handlers — read-only static serving + in-memory ring.
280. 🟡 No HTTP security headers (CSP, `X-Content-Type-Options`, `Referrer-Policy`) are emitted — acceptable for a localhost PoC, flagged for any public deploy.

## 15. Security & Input Validation

281. ✅ All user-controlled strings are HTML-escaped before interpolation (`escapeHtml`, 5-char map).
282. ✅ The audit fragment is the only HTML built from input, and it is fully escaped — no XSS sink.
283. ✅ Request bodies over `MAX_BODY_LEN` (8 KiB) are rejected with 413 — DoS guard.
284. ✅ The size check is double-gated: declared `Content-Length` first, then the actual read (covers lying/chunked bodies).
285. ✅ `action` is validated as a non-empty string and truncated to `MAX_ACTION_LEN` (120).
286. ✅ `detail` is validated as a non-null, non-array object before serialization.
287. ✅ Serialized `detail` is truncated to `MAX_DETAIL_LEN` (400) at parse time, not just at render time.
288. ✅ Truncation is surrogate-pair-safe — never leaves a lone high surrogate (audit fix).
289. ✅ `ts` is range-checked against the valid `Date` window; out-of-range falls back to wall clock.
290. ✅ The ring can never retain a reference to an arbitrarily large hostile payload (constant-bounded entries).
291. ✅ No `eval`, `Function`, or dynamic code execution anywhere.
292. ✅ No `dangerouslySetInnerHTML`-class raw injection on the client beyond the server-escaped fragment.
293. ✅ No SQL / shell / template injection surface (no DB, no shell-out, no template engine on input).
294. ✅ Dependencies are reputable, typed, and pinned via lockfile (supply-chain baseline).
295. ✅ CodeQL `security-extended` scanning runs on push/PR + weekly (`.github/workflows/codeql.yml`).
296. ✅ Dependabot watches npm + GitHub Actions for vulnerable/outdated deps weekly.
297. ✅ A `SECURITY.md` policy exists with a private disclosure path.
298. ✅ The client never trusts server-sent HTML beyond the escaped audit list it requested.
299. ✅ `localStorage` use is namespaced and validated on read (the memory store rejects malformed values).
300. 🟡 No rate-limiting on `POST /api/audit` (single-user localhost; a public deploy would add it).

## 16. Persistence, Memory Store & Audit

301. ✅ Persistence is `localStorage` via a typed `MemoryStore` (`memory/store.ts`), not scattered `setItem` calls.
302. ✅ Reads are validated and finiteness-narrowed; malformed/old values fall back to defaults.
303. ✅ Integer fields reject floats, negatives, NaN, ±Infinity, and non-numbers (`safeInt`-class helper).
304. ✅ Session count increments monotonically and survives reloads.
305. ✅ Persisted state (songIdx, weatherIdx, seed, algoIdx) round-trips deterministically.
306. ✅ The store is unit-tested for round-trips and hostile inputs (`tests/store.test.ts`).
307. ✅ The audit trail is a fixed-size ring on both client and server (caps match: 200).
308. ✅ `AuditTrail.record` is O(1): ring append + fire-and-forget POST.
309. ✅ Audit events are append-only; no in-place mutation of past entries.
310. ✅ Audit POSTs are fire-and-forget — a server hiccup never blocks the sim.
311. ✅ The logger is a shared 512-entry ring with O(1) writes.
312. ✅ Log/audit reads reconstruct chronological order correctly from the circular buffer.
313. ✅ No PII is recorded — events are sim actions and counters.
314. ✅ Storage keys are stable and documented; schema changes degrade gracefully (validated reads).
315. ✅ The store never throws on read — worst case it returns a default.
316. ✅ Audit detail is bounded on the way in (server) and on the way out (escaped render).
317. ✅ Memory store and audit are separable concerns in separate modules.
318. ✅ The audit feed is observable live via the HTMX panel — useful for debugging and demos.
319. ✅ Session/state persistence is the only client-side durable surface — small and auditable.
320. ✅ Tests cover the audit ring eviction and ordering (`tests/audit.test.ts`).

## 17. Testing & Coverage

321. ✅ 29 test files, **453 tests**, ~925k `expect()` assertions — substantial coverage.
322. ✅ Every `src/math/` primitive has a dedicated test (rng, scalar, spatial-hash, quantum, heap).
323. ✅ Every major sim system has a test (entities, phyla, titans/games, connectome/graph-mind, quantum, RD, singularities, weather, atmosphere).
324. ✅ Determinism is golden-tested at the integrated population layer (`tests/determinism.test.ts`).
325. ✅ NaN-stability is tested under sustained max chaos (`tests/nan-stability.test.ts`).
326. ✅ Performance budget has a guard test (`tests/perf-budget.test.ts`).
327. ✅ The heap has property tests: heapsort-equals-Array.sort over 1000 arrays; top-K parity vs a reference stable sort over 2000 rank vectors.
328. ✅ Tie-break parity (rank desc, ascending index) is explicitly tested — the determinism-critical case.
329. ✅ Edge cases are covered (empty, k≤0, k≥n, duplicates, all-equal) for the new DSA code.
330. ✅ Tests use seeded RNG exclusively — reproducible, no flake from randomness.
331. ✅ Render-mode parity (instanced vs not) is pinned (`tests/render-modes.test.ts`, `tests/instanced.test.ts`).
332. ✅ The Observatory snapshot has a 597-line test asserting its structure across pages.
333. ✅ Audio synthesis + analysis are tested (`tests/songs.test.ts`, `tests/audio-analysis.test.ts`).
334. ✅ Quantum register/cloud are tested for normalization and collapse (`tests/quantum*.test.ts`).
335. ✅ Tests run as part of the gate and in CI on every push/PR.
336. ✅ Tests are fast (~1–2 s full suite) — no slow integration drag discouraging runs.
337. ✅ Tests assert behavior and invariants, not implementation trivia.
338. ✅ Reference-implementation cross-checks are used where correctness is subtle (brute-force spatial, full-sort top-K).
339. ✅ A coverage threshold is enforced in CI (`bun run test:coverage`, `bunfig.toml` line ≥ 90% / function ≥ 85%) — measured at ~95.6% line / ~90.5% function, guarded against regression.
340. 🟡 No browser-level e2e (the DOM/WebGL integration is validated via unit-tested data + manual preview, not Playwright).

## 18. Benchmarks & Performance Budget

341. ✅ `bench/` covers the leaf hot paths: rng, scalar, spatial-hash, heap, algorithms, quantum, reaction-diffusion.
342. ✅ Benchmarks use `mitata` on fixed `mulberry32(42)` inputs — comparable run-to-run.
343. ✅ The heap bench directly contrasts `selectTopK` vs full sort+slice at V=10,000, K=20.
344. ✅ `bun run bench` aggregates all suites via `bench/index.ts`.
345. ✅ CI runs benchmarks in a non-blocking job (perf surfaces in logs; runner noise doesn't fail the gate).
346. ✅ The frame budget (16.6 ms) is the stated target; per-stage costs are tabulated in `COMPLEXITY.md`.
347. ✅ The ultra-tier 10k optimization (theory stagger, half-rate flock, ULTRA_GRID_CELL, connectome cadence) is documented in `BENCHMARKS.md`.
348. ✅ The dominant per-frame costs (entity loop, connectome, quantum cloud) are ranked and justified.
349. ✅ Heavy substrates run on cadences chosen so they never collide on a frame.
350. ✅ The reaction-diffusion step is measured (< 0.5 ms at 128²) — claims are numbers, not hand-waving.
351. ✅ The perf-budget test guards against regressions in code, complementing the benches.
352. ✅ Benchmarks are standalone-runnable per suite (`import.meta.main`) for focused profiling.
353. ✅ Allocation discipline (the other half of perf) is a documented, audited contract.
354. ✅ Tier ladder targets (650/2000/5000/10000) match the documented device classes.
355. ✅ The 10k ceiling fills on capable hardware (5070 Ti class), with levers gated above 5k.
356. ✅ No benchmark depends on network or wall-clock for its inputs.
357. ✅ Bench results are interpreted relative to a reference machine (documented), not absolute CI numbers.
358. ✅ The bench job re-installs with the frozen lockfile — reproducible environment.
359. 🟡 No automated perf-regression _threshold_ alert in CI (numbers are informational; a human reads them).
360. 🟡 No continuous historical perf tracking (no stored time-series of bench results).

## 19. Documentation Quality

361. ✅ README is extensive and organized (overview, quickstart, structure, links).
362. ✅ ARCHITECTURE.md describes the system layering and data flow.
363. ✅ MODULE-CONTRACTS.md (78 KB) is a binding spec with signatures and a Known Bugs table.
364. ✅ COMPLEXITY.md is a single-source Big-O budget matching each module's JSDoc.
365. ✅ BENCHMARKS.md records methodology and the ultra-tier optimization.
366. ✅ DESIGN-SYSTEM.md defines tokens, color, type, and layout rules.
367. ✅ ERD.md + the new ERM.md + ERP.md cover structure, relationships, and processes.
368. ✅ WIREFRAMES.md documents the UI layout intent.
369. ✅ PHILOSOPHY.md states the design principles the code is held to.
370. ✅ Five ADRs record the load-bearing decisions (Bun, three.js, HTMX+Tailwind, deterministic RNG, math stack).
371. ✅ CONTRIBUTING.md documents the gate, determinism rule, and allocation discipline.
372. ✅ SECURITY.md defines the disclosure policy.
373. ✅ NOTICE.md + LICENSE cover attribution and licensing.
374. ✅ CHANGELOG.md is kept current per release with real deltas.
375. ✅ KANBAN.md tracks delivery as cards across columns/epics.
376. ✅ ROADMAP.md states shipped/now/next horizons.
377. ✅ This 500-point inspection provides an auditable quality bar.
378. ✅ Every source module carries a header JSDoc explaining its role, complexity, and provenance.
379. ✅ Reference material (math-libs catalog, domain CSV) is preserved under `docs/reference/`.
380. ✅ Docs cross-link rather than duplicate — one fact, one home.

## 20. CI/CD & Automation

381. ✅ CI runs the full gate (format → types → lint → test → build) on push + PR to default branches.
382. ✅ CI pins the Bun version for reproducibility (`1.3.14`, matched to local).
383. ✅ The Bun install cache is keyed on `bun.lock` hash with a restore fallback.
384. ✅ `--frozen-lockfile` install guarantees lockfile authority in CI.
385. ✅ The dist artifact is uploaded with a retention window for inspection.
386. ✅ A non-blocking bench job reports performance without failing the gate.
387. ✅ Concurrency cancels superseded runs on the same ref (no wasted minutes).
388. ✅ Least-privilege `permissions: contents: read` on CI; elevated only where needed.
389. ✅ A release workflow triggers on `v*` tags: gate → build → package site (dist + lab) → publish GitHub Release.
390. ✅ Release notes are auto-generated; the packaged tarball is attached as an asset.
391. ✅ The release path works on private repos (no Pages plan required).
392. ✅ CodeQL security analysis runs on push/PR + a weekly schedule with `security-extended` queries.
393. ✅ Dependabot manages npm + GitHub Actions updates weekly, grouping low-risk dev tooling.
394. ✅ Dependabot uses conventional commit prefixes (`chore(deps)`, `chore(ci)`) and labels.
395. ✅ A PR template enforces the gate checklist + contract/complexity discipline.
396. ✅ Issue templates (bug/feature) + a config with policy links standardize intake.
397. ✅ CODEOWNERS routes review for the composition root, contracts, and CI.
398. ✅ `workflow_dispatch` allows manual runs of CI/CodeQL/Release from the Actions tab.
399. ✅ Timeouts bound every CI job (no runaway minutes).
400. 🟡 No deploy-to-hosting step (the PoC publishes a downloadable build, not a live URL) — intentional for a private repo.

## 21. Dependency & Supply-Chain Hygiene

401. ✅ Dependencies are few and purposeful: three, htmx, mermaid, graphology (+metrics/louvain), d3-delaunay, simple-statistics, simplex-noise, @noble/hashes, fontsource.
402. ✅ Each heavy dependency is justified by an ADR or the math-stack selection doc.
403. ✅ Dev dependencies are clearly separated (prettier, oxlint, tailwind, mitata, typescript, types).
404. ✅ `bun.lock` is committed and used with `--frozen-lockfile` in CI.
405. ✅ Dependabot opens grouped, labeled PRs for updates weekly.
406. ✅ CodeQL covers dependency-adjacent code paths for known vulnerability patterns.
407. ✅ No dependency is loaded from an untrusted or unpinned source.
408. ✅ `@noble/hashes` (audited, dependency-free crypto) backs the sha256 lore derivation.
409. ✅ Crypto-grade hashing is used only for deterministic naming, not security claims — scope is honest.
410. ✅ No transitive `postinstall` script surprises are relied upon by the build.
411. ✅ Font assets are vendored via `@fontsource*` packages, not hot-linked at runtime.
412. ✅ The lab's CDN dependency (p5.js) is isolated to the un-bundled lab artifact, by design.
413. ✅ No deprecated packages in the production dependency set.
414. ✅ Version ranges are lockfile-frozen; reproducible installs.
415. ✅ The dependency list is small enough to audit by hand.
416. ✅ No native/compiled addons — pure JS/TS, portable across platforms.
417. ✅ License compatibility of dependencies is consistent with MIT distribution.
418. ✅ Tree-shaking via the bundler keeps only used code in `dist/`.
419. 🟡 The lab pulls p5.js from a CDN at view time (offline/air-gapped use of the lab requires network) — accepted, documented.
420. ✅ A CycloneDX 1.5 SBOM is generated (`scripts/sbom.ts`, `bun run sbom`) and published as a release asset — deterministic (no timestamp, sorted components), with purls + licenses for every dependency.

## 22. Legal, Licensing & Compliance

421. ✅ A `LICENSE` file is present (MIT).
422. ✅ `NOTICE.md` records attributions and third-party notices.
423. ✅ The project was renamed off any trademarked source and carries original branding.
424. ✅ The verbatim legacy origin is preserved with provenance, not silently absorbed.
425. ✅ No bundled third-party code is relicensed or stripped of its notices.
426. ✅ Dependency licenses are MIT/BSD/Apache-class, compatible with MIT redistribution.
427. ✅ No proprietary assets are checked in without rights.
428. ✅ `SECURITY.md` provides a responsible-disclosure path.
429. ✅ No telemetry leaves the machine — no privacy/consent surface to manage.
430. ✅ No user accounts, PII, or data collection — GDPR/CCPA scope is effectively nil.
431. ✅ Generated art/lore is original and procedurally derived, not copied from a protected corpus.
432. ✅ Commit history is honest and attributable (Co-Authored-By trailers where applicable).
433. ✅ The CHANGELOG provides an auditable record of what shipped when.
434. ✅ License headers/attribution are not stripped from vendored fonts.
435. ✅ The repo makes no unlicensed performance/medical/financial claims.
436. ✅ ADRs document why each load-bearing third-party choice is acceptable.
437. ✅ The README states the license and usage terms plainly.
438. ✅ No export-controlled cryptography (sha256 for naming only).
439. ✅ Trademarks/brands referenced in flavor text are clearly creative, not endorsements.
440. ✅ The provenance trail (legacy/opus-4.7-origin) supports any future authorship question.

## 23. Developer Experience & Contribution

441. ✅ One command sets up nothing beyond `bun install`; one command runs everything (`bun run check`).
442. ✅ `bun dev` gives instant HMR for iterative work.
443. ✅ The cwd requirement and bracketed-path gotchas are documented (memory + contracts).
444. ✅ CONTRIBUTING.md states the gate, determinism rule, and allocation discipline up front.
445. ✅ The PR template encodes the review checklist so contributors self-serve.
446. ✅ Issue templates lower the bar to good bug reports/feature requests.
447. ✅ CODEOWNERS makes review routing explicit.
448. ✅ Module contracts give a contributor exclusive-ownership boundaries for parallel work.
449. ✅ JSDoc-on-every-module means a newcomer can read intent without spelunking.
450. ✅ Consistent naming + small modules keep cognitive load low.
451. ✅ Tests are fast, so the inner loop (edit → check) is tight.
452. ✅ The complexity budget tells a contributor where the performance lines are.
453. ✅ ADRs explain "why this, not that" so decisions aren't re-litigated.
454. ✅ The design system removes bikeshedding over colors/spacing.
455. ✅ Errors in the gate are specific and actionable (tsc/oxlint/prettier messaging).
456. ✅ The dev server's HMR `[object Object]` noise is documented as benign (saves a debugging dead-end).
457. ✅ The benchmark suite is runnable per-file for focused profiling.
458. ✅ The repo is self-documenting enough to onboard without a maintainer call.
459. 🟡 Bus factor is 1 (single author) — mitigated by exhaustive docs/contracts, but a real org risk.
460. ✅ A `.editorconfig` + Prettier means contributors don't fight formatting.

## 24. Error Handling & Resilience

461. ✅ The server returns precise status codes (400/413/404) with JSON error bodies, never a bare 500 on bad input.
462. ✅ Audit rendering is fault-isolated per entry — one bad slot degrades gracefully.
463. ✅ JSON parse and body read are both try/caught with 400s.
464. ✅ Oversized bodies are rejected before allocation-heavy parsing.
465. ✅ The memory store never throws on read — it returns validated defaults.
466. ✅ Audit POSTs are fire-and-forget, so server unavailability never blocks or crashes the sim.
467. ✅ NaN/Inf are contained at their sources (clamps), not allowed to propagate into a crash.
468. ✅ The rAF loop is resilient to a backgrounded tab (pauses cleanly, resumes correctly).
469. ✅ WebGL state is reconstructable from the sim source of truth after a context blip.
470. ✅ Audio no-ops safely when locked/unavailable rather than throwing.
471. ✅ Out-of-range timestamps fall back rather than throwing in `toISOString`.
472. ✅ Index-out-of-range is structurally impossible to ignore (`noUncheckedIndexedAccess`).
473. ✅ Unknown routes return a clean 404, not a stack trace.
474. ✅ The logger ring can't overflow memory (fixed size).
475. ✅ Surrogate-safe truncation prevents permanent mojibake corruption of the audit feed.
476. ✅ Defensive `?? 0` / `?? default` reads guard against missing external-object keys.
477. ✅ The build fails fast and loud rather than emitting a broken bundle.
478. ✅ Determinism + golden tests turn many would-be runtime errors into caught test failures.
479. ✅ No empty catch blocks that swallow errors silently in meaningful paths.
480. ✅ Long-run stability is asserted (no NaN, no leak) by the heavy-substrate tests.

## 25. Release Management & Versioning

481. ✅ Semantic versioning is used; `package.json` `version` is the canonical source.
482. ✅ The server's reported version is now **derived from `package.json` at startup** — the prior hand-synced constant (a real drift defect: server said `0.6.1` while the package was `0.7.2`) is eliminated.
483. ✅ The CHANGELOG follows a keep-a-changelog-style per-release structure with real deltas.
484. ✅ Releases are cut by tagging `vX.Y.Z`; the release workflow gates, builds, packages, and publishes.
485. ✅ Release notes are auto-generated and the packaged build is attached.
486. ✅ Each release is preceded by a full-gate pass (`bun run check`) — green or it doesn't ship.
487. ✅ The 500-point inspection is re-run before each tagged release; deltas recorded.
488. ✅ Version history is legible in `git log` with conventional, descriptive messages.
489. ✅ The "beyond" releases (0.4 XENOGENESIS → 0.5 RESONANCE → 0.6 ATELIER → 0.7 → 0.8) tell a coherent story.
490. ✅ Breaking vs additive changes are distinguishable from commit/CHANGELOG conventions.
491. ✅ Tags map 1:1 to CHANGELOG entries and to KANBAN "Done" cards.
492. ✅ The release artifact (dist + lab) is a self-contained, runnable PoC per version.
493. ✅ Dependabot/CI keep the dependency floor current between releases.
494. ✅ No release depends on undocumented manual steps — the workflow is the procedure.
495. ✅ Rollback is trivial: tags are immutable, prior release assets remain downloadable.
496. ✅ The version is asserted reachable via `GET /api/health` for deployment smoke checks.
497. ✅ Pre-release hardening (this audit, DSA upgrades, CI/governance) is itself versioned and changelogged.
498. ✅ The project state is reproducible from any tag via `bun install --frozen-lockfile && bun run check`.
499. ✅ Documentation versions move in lockstep with code (docs updated in the same release).
500. ✅ The repository, end to end (idea → build → PoC/MVP → audit → release), is internally consistent and self-evidencing.

---

### How to use this inspection

- **Before a release:** re-walk the 16 🟡 WARNs; promote any that have been resolved to ✅, and add new
  points as the surface grows. A WARN flipping to ❌ blocks the tag.
- **During review:** cite point numbers in PR comments (e.g. "regresses §6.101 allocation discipline").
- **For newcomers:** read this top-to-bottom for a guided tour of what "done" means here, then dive into
  [MODULE-CONTRACTS.md](./MODULE-CONTRACTS.md) for the binding detail.

The 16 WARNs are deliberate, named limitations appropriate to a **local, single-author proof of
concept** — none is a correctness, determinism, or security defect. They are the honest edge of the
envelope, recorded so they are decisions rather than blind spots.
