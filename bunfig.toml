[serve.static]
plugins = ["bun-plugin-tailwind"]

# Coverage policy lives only in scripts/canonical-receipts.ts and is enforced by verify-receipts.ts.
# Duplicating numeric thresholds here previously made Ubuntu's valid portable floor exit non-zero while
# a shell pipeline silently discarded that status. One source of truth; one child process; one exit code.
