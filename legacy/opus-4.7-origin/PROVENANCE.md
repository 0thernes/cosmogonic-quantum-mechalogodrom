# Opus 4.7 origin — preserved records

This folder preserves the **unique** records from the original
Cosmogonic Quantum Mechalogodrom, which existed as a separate single-file
project before this repository was built.

## What it was

- Location (now removed): `Z:\[Vibe Coded (AI)]\CLAUDECODE\Cosmogonic Quantum Mechalogodrom\`
  — note: on 2026-06-11, *after* the old folder was deleted, **this repository
  itself was moved to that same path** (it previously lived one level deeper,
  under a since-removed `Garbage Code Slop\` folder). So today that path is this
  repo — the old single-file project is gone, not hiding here.
- Built by **Claude Opus 4.7**, dated **2026-05-17 … 2026-05-22** (4 commits).
- Contents: a single `cosmogonic-quantum-mechalogodrom.html` (+ a byte-identical
  `index.html` copy), a short `README.md`, a development-history markdown, and
  its own git repo.

## What was merged here, and why

The original `.html` is **byte-identical** (SHA verified) to this repo's
[`../cosmogonic-quantum-mechalogodrom.html`](../cosmogonic-quantum-mechalogodrom.html) —
the source this whole repository was ported from. So the code itself was already
preserved; the old folder's `.html`, its duplicate `index.html`, and its stale
`.gitignore` were exact/near duplicates and were not copied.

What is preserved here is everything the old folder had that this repo did **not**:

| File | What it is |
| --- | --- |
| `CLAUDE-OPUS-4.7-DEVELOPMENT-HISTORY.md` | The Opus 4.7 development log — titled iterations of the original build. |
| `README-original.md` | The original single-file README ("Vibe Coded AI — segmented master branch"). |
| `GIT-HISTORY.txt` | A text export of the old git repo's 4 commits (the git history itself was not grafted in). |

After this merge the old folder was deleted (it was a same-named duplicate of
this project at a shallower path, which was causing confusion). Nothing unique
was lost — it lives here, in the descendant project's history.
